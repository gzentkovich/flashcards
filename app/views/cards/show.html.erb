<%= title @cardback %>
<div class="row">
  <div class="small-12 columns">
    <fieldset>
    <legend>Back</legend>
    <p> <%= @card.back %> </p>
    <p> <%= @card.front %> </p>
      <%= link_to "Back", @deck, :class => "small button secondary radius" %><br />
    </fieldset>
  </div>
</div>
